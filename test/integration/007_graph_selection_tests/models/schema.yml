version: 2
models:
- name: emails
  columns:
  - name: email
    tests:
    - unique
- name: users
  columns:
  - name: id
    tests:
    - unique
- name: users_rollup
  columns:
  - name: gender
    tests:
    - unique
